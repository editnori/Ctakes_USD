// Thread-safe Default Temporal + Coref pipeline (XMI only)

threads 3

// Temporal stack (includes TsDefaultFast)
load TsDefaultTemporalPipeline

// Add thread-safe coreference resolution
load TsCorefSubPipe

// Write XMI under subdirectory
add FileTreeXmiWriter SubDirectory=xmi

// Finished log
addLast util.log.FinishedLogger

