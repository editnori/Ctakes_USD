// Thread-safe Default Temporal pipeline (XMI only)

threads 3

// Thread-safe clinical baseline (tokens, chunks, dictionary, assertion)
load TsDefaultFastPipeline

// Thread-safe temporal events + relations
load TsTemporalSubPipe

// Write XMI under subdirectory
add FileTreeXmiWriter SubDirectory=xmi

// Finished log
addLast util.log.FinishedLogger

