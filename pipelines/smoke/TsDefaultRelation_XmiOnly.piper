// Thread-safe Default Relation pipeline (XMI only)

threads 3

// Start from the full thread-safe fast pipeline
load TsDefaultFastPipeline

// Add thread-safe clinical relations (degree/location/modifier)
load TsRelationSubPipe

// Write XMI under subdirectory
add FileTreeXmiWriter SubDirectory=xmi

// Finished log
addLast util.log.FinishedLogger

